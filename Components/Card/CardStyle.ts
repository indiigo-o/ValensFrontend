import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  movie: {
    padding: '20px',
    float: 'right',
    height: '20%',
    width: '20%',
  },
  poster: {
    height: '100%',
    width: '100%',
  },
  container: {
    height: '100%',
    width: '100%'
  },
  moviedetails: {
    fontSize: '12px',
  }
});

export default useStyles;